# {{PROJECT_NAME}} — PLATFORM BRAINS HUB (NOT YET ACTIVATED)

**Status:** This folder was scaffolded by `ab init` on {{TODAY}} in **hub mode** and is **not yet activated**. The `.platform/` context pack is present but the sibling repo list and cross-repo conventions are empty.

> {{DESCRIPTION}}

---

## What is a "platform brains hub"?

This folder is **not a code repo**. It exists to hold the `.platform/` context pack that coordinates multiple sibling repos (for example, a backend + a frontend + a widget living in neighbouring folders). The hub is where cross-repo architecture, decisions, conventions, and workflow live so every AI session across every repo reads from the same source of truth.

You'll see folders next to this one like `../my-backend`, `../my-frontend`, `../my-widget` — those are the repos this hub coordinates. The hub itself holds only `.platform/`, this `CLAUDE.md`, and optionally a README.

---

## You are an AI agent reading this file

Claude Code, Codex CLI, Gemini CLI, or any other AI tool — this file is auto-loaded when you open this hub folder. You were pointed here because it lives at the hub root.

**When the user says "activate this project"** (or similar — "fill in the platform pack", "onboard this hub", "run ab activation"), read `.platform/ACTIVATE-HUB.md` and follow its 6-step protocol. That file is the **hub-mode** activation protocol — it scans sibling repo paths listed in `.platform/repos.md`, not this (empty) hub folder.

**Until activation**, answer the user's question normally. If relevant, mention that the hub has not been activated yet and they can say "activate this project" to run the setup.

**Important:** `.platform/ACTIVATE-HUB.md` contains rules for how to handle existing `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` files at the root (here and in every sibling repo) — **never delete user content**, always prepend the ab section to the top and preserve the original below.

---

## Next steps for the human

1. Open `.platform/repos.md` and list the sibling repos you want this hub to coordinate. Replace the placeholder rows with real paths, names, and stacks.
2. For each sibling repo, run `ab add-repo <path>` from this hub folder — that scaffolds a thin `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` in the sibling that points back at this hub's `.platform/` pack.
3. Open this hub folder in your AI CLI and say `activate this project`. The LLM will read `.platform/ACTIVATE-HUB.md`, scan each sibling repo, interview you for a few minutes, and fill in the hub pack.

---

## Active Work (`.platform/work/`)

**Read `.platform/work/ACTIVE.md` first every session** — before STATUS.md, before anything else.
It tells you what's currently in progress so you can orient in seconds, not minutes.
See `.platform/agents/work-tracking.md` for the full protocol.

## Reference pack

- `.platform/ACTIVATE-HUB.md` — the hub-mode activation protocol (read this when the user says "activate")
- `.platform/repos.md` — the sibling repo list (you fill this in first)
- `.platform/workflow.md` — the 6-stage inline workflow for non-trivial tasks
- `.platform/ONBOARDING.md` — the 7-step onboarding path for future sessions
- `.platform/scripts/sync-context.sh` — keeps root entry files in sync across repos (see `ACTIVATE-HUB.md` Step 5)

## Subagent Model Assignment (Claude Code only)

Before every `Task` tool dispatch, print a manifest block in chat:

> **⚡ Dispatching N agent(s)**
>
> | Agent | Model | Task |
> |---|---|---|
> | **Name** | 🔵 `sonnet` | purpose |

Model emoji: 🔵 `sonnet` · 🟣 `opus` · 🟡 `haiku`. Pass `model` explicitly on every `Task` call — never omit it (omitting inherits Opus and wastes tokens).

| Work type | Model |
|---|---|
| Research, audit, exploration, doc writing, code review, test writing, security audit | 🔵 **`sonnet`** |
| Code implementation, hard architectural decisions, executors handling deviations | 🟣 **`opus`** |
| Mechanical / trivial narrow operations | 🟡 **`haiku`** |

Name agents with a persona — e.g., "Max the Mapper", "Kai the Coder". Full rule: `.platform/agents/subagent-dispatch.md`.

## Skills available to you

Installed additively into `.claude/skills/` by `ab init`. Prefixed `ab-` so they never collide with gstack / pre-existing skills.

`ab-triage`, `ab-workflow`, `ab-research`, `ab-pm`, `ab-architect`, `ab-test-writer`, `ab-security`, `ab-qa`, `ab-review`, `ab-debug`

Read each skill's `SKILL.md` on first use to understand its protocol.

---

_Generated by ab (hub mode) on {{TODAY}}. After activation, this file will be replaced with the steady-state hub version (or, if you had an existing `CLAUDE.md` before init, the steady-state content will be prepended to the top of your file)._
